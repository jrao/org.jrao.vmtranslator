# Compiling

To compile the VM translator, run ./gradlew build from the project's root directory

# Running

To run the VM translator, run java -jar build/libs/org.jrao.vmtranslator.jar [path-to-vm-file-or-directory]
